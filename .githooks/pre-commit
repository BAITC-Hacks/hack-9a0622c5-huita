#!/bin/sh
set -eu
repo_root=$(git rev-parse --show-toplevel)
cd "$repo_root"
if [ -x .venv/bin/python ]; then
    exec .venv/bin/python scripts/check_repository.py --staged
fi
exec python3 scripts/check_repository.py --staged
