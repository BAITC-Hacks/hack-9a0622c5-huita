"""Participant entry point; run with the unmodified official local_eval.py."""

from beesmart.core.runner import CampaignAgent


class Agent(CampaignAgent):
    """Beesmart's reproducible, pilot-driven tariff campaign agent."""
